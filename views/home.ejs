<%# 
we have changed the location of header and footer file from views to partial thus we need to change the path as per the need
%>

<%# 
we have changed the location of header and footer file from views to partial thus we need to change the path as per the need
%>

<%-include("partials/header")-%>

<h1>Home</h1>
<p><%=startingContent%></p>

<% posts.forEach(function(post){ %>
    <% if (typeof post.content == 'undefined' ) { %> 
        <p> no content</p> 
        <% }else { %>
            <h1><%= post.title  %></h1>
            <p><%= post.content.substring(0,100) +"..." %>
                <a href="/posts/<%=post._id%>">Read more</a>
            </p>

        <% } %>

<% }); %>

<%-include("partials/footer")-%>
